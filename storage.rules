rules_version = '2';
service firebase.storage { match /b/{bucket}/o { function signedIn(){return request.auth!=null;} function owner(){return signedIn()&&request.auth.uid=='o6rMhcEyUXUstmb7ptClXSv8ejQ2';} match /companies/arw2026/{all=**}{ allow read: if signedIn(); allow write: if owner() || (signedIn() && request.resource.size < 15 * 1024 * 1024); } match /{all=**}{allow read,write: if false;} } }
